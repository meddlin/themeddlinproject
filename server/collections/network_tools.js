/*
 * Add query methods like this:
 *  NetworkTools.findPublic = function () {
 *    return NetworkTools.find({is_public: true});
 *  }
 */

NetworkTools.allow({
  insert: function (userId, doc) {
    return true;
  },

  update: function (userId, doc, fieldNames, modifier) {
    return true;
  },

  remove: function (userId, doc) {
    return true;
  }
});

NetworkTools.deny({
  insert: function (userId, doc) {
    return false;
  },

  update: function (userId, doc, fieldNames, modifier) {
    return false;
  },

  remove: function (userId, doc) {
    return false;
  }
});
